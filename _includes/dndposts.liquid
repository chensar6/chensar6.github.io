<div class="col">
  <a href="{% if dndpost.redirect %}{{ dndpost.redirect }}{% else %}{{ dndpost.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if dndpost.img %}
        {%
          include figure.liquid
          loading="eager"
          path=dndpost.img
          sizes = "250px"
          alt="dndpost thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ dndpost.title }}</h2>
        <p class="card-text">{{ dndpost.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if dndpost.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ dndpost.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if dndpost.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ dndpost.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
